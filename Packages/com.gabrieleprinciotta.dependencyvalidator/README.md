# Dependency Validator

A small utility package with a static class called `DependencyValidator`.

This class provides utility methods for validating object dependencies and component existence in Unity.